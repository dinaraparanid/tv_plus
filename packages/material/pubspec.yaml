name: tv_plus_material
description: "Smart TV toolkit"
version: 0.5.1
repository: https://github.com/dinaraparanid/tv_plus
resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  tv_plus_foundation: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  flutter_gen_runner: ^5.11.0
  flutter_pre_commit: ^0.2.4
