import 'package:flutter/material.dart';

import 'tab/tv_tab_bar_foundation_sample.dart';

final class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return const TvTabBarFoundationSample();
  }
}
