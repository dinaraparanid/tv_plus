import 'package:flutter/material.dart';

import 'cupertino/navigation/tab_bar_sample.dart';

final class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return const CupertinoTvTabBarSample();
  }
}
