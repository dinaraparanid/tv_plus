import 'package:flutter/material.dart';

import 'scroll/sliver_tv_list_sample.dart';

final class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return const SliverTvListSample();
  }
}
